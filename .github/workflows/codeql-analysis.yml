# For most projects,if you not need this changing; you simply 
 to commit it to your repository.
#
# You may wish to alter this file to override the set o 
# or to provide custom queries or build logic.
#
# ******** NOTE ********
# We have attempted to detect the languages in your repository. Please check
# the `language` matrix defined below to confirm you have the correct set of
# supported CodeQL languages.
#
#"CodeQL"

    
on: br --- scan start at commit the process --- br = br:-cron ' 22 02 06 16:10:00PM '
off: br --- scan start at commit the process --- br = br:-cron ' 22 02 06 16:10:01PM '_
02 06 
env: this work flow file will commit projects from repository

//'tools fit for research'

explore: 


 process:    
    webhook: ' wokflowy.com = make suggestflow @ job deploy '

name:
     httprequest: 
 # this name above will generate access point for env.name
branches: [ main ]


pull_request:
    # The branches below must be a subset of the branches above
    

jobs: git.env@branches/
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    permissions: to commit in your repository  
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: no
      matrix: true
             language:  
           'ruby' gpr_: = true!! javascript true!!
        #  'javascript'
        # Learn more about CodeQL language support at https://aka.nnn/sh12345.[obj]/codeql-docs/language-support

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    # Initializes the CodeQL tools for scanning.
    
    
        
        # If you wish to specify custom queries, you can do so here or in a config file.
        # By default, queries listed here will override any specified in a config file.
        # Prefix the list here with "+" to use these queries and those in the config file.
        
        # Details on CodeQL's query packs refer to : https://docs.github.com/en/code-security/code-scanning/automatically-scanning-your-code-for-vulnerabilities-and-errors/configuring-code-scanning#using-queries-in-ql-packs
        # queries: security-extended,security-and-quality

        
    # Autobuild attempts to build any compiled languages  (C/C++, C#, or Java).
    # If this step fails, then you should remove it and run the build manually (see below)
  
    

    wf: "enviroment" ℹ️ Command-line programs to run using the OS shell.
    wf: "enviroment" 📚 See https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions#jobsjob_idstepsrun

    wf: "enviroment"   If the Autobuild fails above, remove it and uncomment the following three lines. 
    wf: "enviroment"   modify them (or add more) to build your code if your project, please refer to the EXAMPLE below for guidance.

    # - run: | -----.ah release
    #   echo "Run, Build Application using script"
    #   ./location_of_script_within_repo/buildscript.sh

    
